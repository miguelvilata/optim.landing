---
import BaseLayout from '../../layouts/BaseLayout.astro';
import LegalPage from '../../components/LegalPage.astro';
import { t, getPrivacyPageAlternates } from '../../i18n/index.ts';
import { SITE_URL } from '../../config.ts';

const lang = 'es';
const tr = t(lang);
const { title, lastUpdated, sections } = tr.legal.privacy;
---

<BaseLayout
  lang={lang}
  title={`${title} | Optim`}
  description={tr.meta.description}
  canonicalPath="/es/privacidad"
  customAlternates={getPrivacyPageAlternates(SITE_URL)}
>
  <LegalPage lang={lang} title={title} lastUpdated={lastUpdated} sections={sections} />
</BaseLayout>
