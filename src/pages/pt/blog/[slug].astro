---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import BlogPost from '../../../components/BlogPost.astro';
import { t } from '../../../i18n/index.ts';
import { SITE_URL } from '../../../config.ts';
import type { Lang } from '../../../i18n/index.ts';

export async function getStaticPaths() {
  const now = new Date();
  const posts = await getCollection('blog', (post) =>
    post.data.lang === 'pt' && !post.data.draft && post.data.pubDate <= now
  );
  return posts.map((post) => {
    const urlSlug = post.slug.split('/').at(-1)!;
    return {
      params: { slug: urlSlug },
      props: { post, urlSlug },
    };
  });
}

const { post, urlSlug } = Astro.props;
const { Content } = await post.render();
const lang = 'pt' as Lang;
const tr = t(lang);

const esSlug = post.data.translations?.es;
const enSlug = post.data.translations?.en;

const customAlternates = [
  { lang: 'es' as Lang, url: esSlug ? `${SITE_URL}/es/blog/${esSlug}` : `${SITE_URL}/es/blog` },
  { lang: 'en' as Lang, url: enSlug ? `${SITE_URL}/en/blog/${enSlug}` : `${SITE_URL}/en/blog` },
  { lang: 'pt' as Lang, url: `${SITE_URL}/pt/blog/${urlSlug}` },
];
---

<BaseLayout
  lang={lang}
  title={`${post.data.title} — ${tr.blog.title}`}
  description={post.data.description}
  canonicalPath={`/pt/blog/${urlSlug}`}
  customAlternates={customAlternates}
>
  <BlogPost
    lang={lang}
    title={post.data.title}
    description={post.data.description}
    pubDate={post.data.pubDate}
    updatedDate={post.data.updatedDate}
    author={post.data.author}
    tags={post.data.tags}
    image={post.data.image}
  >
    <Content />
  </BlogPost>
</BaseLayout>
